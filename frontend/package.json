{
  "name": "frontend",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "bun run --bun vite",
    "build": "bun run --bun vite build --minify false --mode development",
    "build:prod": "bun run --bun vite build --mode production",
    "preview": "bun run --bun vite preview"
  },
  "devDependencies": {
    "@wailsio/runtime": "latest",
    "daisyui": "^4.6.1",
    "tailwindcss": "^3.4.1",
    "vite": "^5.0.0"
  }
}