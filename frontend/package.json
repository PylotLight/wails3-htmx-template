{
  "name": "frontend",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "bunx --bun astro dev --port 5173",
    "start": "bunx --bun astro dev --port 5173",
    "build": "bunx --bun astro build",
    "preview": "bunx --bun astro preview"
  },
  "type": "module",
  "dependencies": {
    "@astrojs/tailwind": "^5.0.3",
    "tailwindcss": "^3.3.6",
    "astro": "^4.0.5",
    "tailwind": "^4.0.0"
  }
}